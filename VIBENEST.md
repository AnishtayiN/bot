# 🌐 اجرای ربات روی VibeNest (و هر پلتفرم مشابه: Railway / Render / Fly / Docker)

> TL;DR — سرویس را وصل کن، دو متغیر `BOT_TOKEN` و `ADMIN_IDS` را بگذار، Deploy بزن.
> بعد از دیپلوی، آدرس `https://<دامنه‌ی-سرویس>/health` را در مرورگر باز کن؛
> **همه‌چیز** (ربات وصل شده؟ چند پیام گرفته؟ مشکل کجاست؟) همان‌جا نوشته می‌شود.

---

## 🩺 چرا این نسخه دیگر ۴۰۴ نمی‌دهد؟

پلتفرم‌ها یک پورت را با متغیر `PORT` اعلام می‌کنند و Healthcheck را روی همان پورت
می‌زنند. نسخه‌ی قبلی ربات **هیچ سرویس HTTP‌ای بالا نمی‌آورد** (فقط یک health ساده و
مشروط به `PORT`)؛ اگر پورت گوش نمی‌داد یا متغیرهای ضروری ست نبودند، پروسه با خطا
می‌مُرد و مسیر/دامنه «Unreachable» می‌شد — نتیجه: `404 page not found` و ربات مرده.

حالا در `bot.py`:

| کار | نتیجه |
|---|---|
| وب‌سرور وضعیت **همیشه اول از همه** روی `0.0.0.0:PORT` بالا می‌آید | Healthcheck پلتفرم پاس می‌شود و دامنه بالا می‌ماند |
| مسیرهای `/` ، `/health` ، `/healthz` ، `/status` ، `/ping` ، `/ready` + پشتیبانی HEAD | هر Healthcheckی را جواب می‌دهد |
| اگر پلتفرم `PORT` ندهد، روی 3000/8080/8000/5000/80 گوش می‌دهد | حالت «پورت تشخیص داده نشد» هم پوشش داده شده |
| نبود `BOT_TOKEN`/`ADMIN_IDS` پروسه را نمی‌کُشد؛ صفحه‌ی وضعیت می‌گوید چه چیزی کم است | به‌جای 404، خودِ دامنه راهنمای رفع مشکل را نشان می‌دهد |
| تست `getMe`/`getWebhookInfo` قبل از اجرا + حذف خودکار وب‌هوکِ قدیمی | علت‌های رایج «ربات جواب نمی‌دهد» پیدا/رفع می‌شود |
| خطاهای Conflict / NetworkError با راهنمای فارسی و هشدار به ادمین در تلگرام | می‌فهمی مشکل از دو نسخه‌ی هم‌زمان است یا از شبکه |
| polling با `bootstrap_retries=-1` + حلقه‌ی تلاش مجدد | قطعی موقت شبکه پروسه را نمی‌کُشد |
| مسیر داده‌ی قابل‌نوشتن‌نبودن (`/data` بدون Volume) → fallback خودکار | crash نمی‌کند |
| سرور PO-Token (deno) روی کانتینر کم‌حافظه خودکار خاموش می‌شود | روی پلن رایگان ۲۵۶MB با OOM کشته نمی‌شود |

قبل از این هم: توکنی که در `.env.example` منتشر شده بود را در
[@BotFather](https://t.me/BotFather) → `/revoke` عوض کن و توکن جدید را در متغیرها بگذار.

---

## 🚀 مرحله‌به‌مرحله

1. در داشبورد VibeNest → **GitHub repository URL** → آدرس همین ریپو را بده و
   (اگر خصوصی است) اپ GitHub را وصل کن.
2. VibeNest خودش `Dockerfile` را تشخیص می‌دهد. اگر پرسید، همان را انتخاب کن.
3. در بخش **Variables** این‌ها را ست کن:

   | متغیر | مقدار | لازم؟ |
   |---|---|---|
   | `BOT_TOKEN` | توکن از @BotFather | ✅ |
   | `ADMIN_IDS` | آیدی عددی خودت (از @userinfobot) | ✅ |
   | `FORCE_CHANNELS` | `@yourchannel` (اختیاری، با اسپیس برای چندتا) | ➖ |
   | `LOG_CHANNEL` | `@yourlogchannel` برای گزارش خطاها | ➖ |
   | `COOKIES_B64` | خروجی `base64 -w0 cookies.txt` برای یوتیوب محدودشده | ➖ |
   | `MAX_CONCURRENT_DL` | روی پلن کم‌رم `1` بگذار | ➖ |

4. **Deploy**. بعد از بالا آمدن، `https://<دامنه‌ی-سرویس>/health` را باز کن.

---

## 📊 خواندن صفحه‌ی `/health`

```json
{
  "status": "polling",                 ← polling | webhook | config_error | retrying | stopped
  "ok": true,
  "bot": "@MyMusicBot",
  "telegram": {
    "api": "ok",                       ← ok | unreachable | conflict | error
    "updates_seen": 37,
    "last_update_ago_sec": 4,
    "last_error": null
  },
  "memory": {"rss_mb": 141.8, "limit_mb": 256.0},
  "yt_dlp": "2026.08.19",
  "ports": [3000]
}
```

| چیزی که می‌بینی | معنی و راه‌حل |
|---|---|
| `"api": "ok"` و `updates_seen` بالا می‌رود | ربات سالم است ✅ |
| `"status": "config_error"` | `config_problems` می‌گوید کدام متغیر را در Variables اضافه کنی |
| `"api": "unreachable"` | سرور به تلگرام دسترسی ندارد: یا توکن/اینترنت سرویس را چک کن، یا روی سرور ایران `PROXY_URL` بگذار |
| `"api": "conflict"` | همان توکن روی یک سرور/دیپلوی دیگر هم روشن است — یکی را خاموش کن (این کار باعث می‌شود پیام‌ها بین دو نسخه گم شوند) |
| `memory.rss_mb` نزدیک `limit_mb` | پلتفرم ممکن است کانتینر را kill کند: `POT_SERVER=0`، `MAX_CONCURRENT_DL=1` یا پلن با RAM بیشتر |
| `"status": "retrying"` | خطا خورده و دارد دوباره تلاش می‌کند؛ `telegram.last_error` را ببین |

---

## 🔗 حالت وب‌هوک (اختیاری، اما توصیه‌شده روی پلتفرم)

پیش‌فرض `polling` است (ربات خودش از تلگرام پیام می‌گیرد). اگر شبکه‌ی خروجی سرویس
محدود است یا دوست داری تلگرام خودش آپدیت‌ها را به دامنه بفرستد:

```env
WEBHOOK_URL=https://bot.vibenest.net
# WEBHOOK_PATH=telegram        # پیش‌فرض
# WEBHOOK_SECRET=...           # خودکار از توکن ساخته می‌شود
```

بعد از ری‌دیپلوی، تلگرام آپدیت‌ها را به `https://bot.vibenest.net/telegram` می‌فرستد
(مسیر با یک secret token محافظت می‌شود) و خودِ صفحه‌ی `/health` هم بالاست.

> اگر `WEBHOOK_URL` نگذاری، `AUTO_CLEAR_WEBHOOK=1` (پیش‌فرض) هر وب‌هوک قدیمیِ ست‌شده روی
> توکن را پاک می‌کند تا polling بدون خطای `Conflict` کار کند.

---

## 🧦 پروکسی (برای سرورهای ایران)

```env
PROXY_URL=socks5://user:pass@host:port
# یا
PROXY_URL=http://host:port
```

این پروکسی هم برای ترافیک تلگرام استفاده می‌شود و هم برای yt-dlp (چون ارورهای شبکه
را از همین متغیر می‌خواند: `HTTPS_PROXY`/`ALL_PROXY` هم قبول است).

---

## 🧠 حافظه و پلن رایگان (۲۵۶MB)

- ربات خالی حدود **۹۰MB** رم می‌گیرد؛ هر دانلود همزمان با ffmpeg می‌تواند صدها مگابایت
  بخواهد. روی پلن رایگان: `MAX_CONCURRENT_DL=1` بگذار و `POT_SERVER=auto` را نگه دار
  (خودش روی کانتینر کم‌حافظه سرور deno را خاموش می‌کند).
- اگر خیلی قطعی می‌شود یا `rss_mb` به `limit_mb` می‌چسبد، پلن **Starter (1GB)** یا بالاتر بگیر.
- بدون سرور PO-Token ممکن است بعضی ویدیوهای یوتیوب خطای «Sign in to confirm» بدهند؛
  راه‌حل: `COOKIES_B64` یا پلن با رم بیشتر.

---

## 🧪 تست محلی بدون تلگرام

```bash
python tests/smoke_http.py
```

این تست بدون توکن واقعی، بالا آمدن وب‌سرور، مسیرهای Healthcheck، امنیت مسیر وب‌هوک و
رسیدن یک آپدیت به صف پردازش را چک می‌کند.

---

## 🆘 اگر باز هم کار نکرد

1. `https://<دامنه>/health` را باز کن و متن `hint`/`last_error` را برایم بفرست.
2. لاگ سرویس (Logs) را از داشبورد کپی کن — خط‌های تیک‌دار ✅/❌ فارسی دقیقاً می‌گویند
   کدام مرحله شکسته است.
3. چک کن ربات دیگری با همین توکن روی Railway/لوکال/VPS روشن نباشد (خطای `Conflict`).
